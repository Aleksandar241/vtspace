// Paths
const userPath = 'user';
const postsPath = 'posts';
const loginPath = 'login';
const signupPath = 'signup';
const logoutPath = 'logout';

// Query keys
const userKey = 'user';
const postsKey = 'posts';

export { userPath, postsPath, loginPath, signupPath, logoutPath, userKey, postsKey };
